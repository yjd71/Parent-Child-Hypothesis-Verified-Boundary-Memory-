"""
Shared data structures for the prototype-guided branch.

Planned contents:
- Prototype bank state containers.
- Retrieval outputs such as `sim_fg`, `sim_bg`, `fu_fg`, `fu_bg`.
- Interaction outputs such as `alpha(x)`, `theta`, `M(x)`, `H(x)`.
- Fusion outputs for labeled and unlabeled rectified predictions.
"""
